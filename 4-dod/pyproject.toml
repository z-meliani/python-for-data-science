[project]
name = "4-dod"
version = "0.1.0"
description = "Data Oriented Design"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
]

[tool.setuptools]
packages = []
